# Build output
/build/
/build-rel/
/build-st/
# -DEDEN_THREADED=ON, audit row 36/C1 — see WORKING/c1-threaded-build-handoff.md
/build-thr/
/dist/
*.wasm
*.wasm.map
*.o
*.a

# Emscripten SDK (if vendored locally)
/emsdk/

# Node
/node_modules/

# Symlinks (re)created by CMakeLists.txt's file(CREATE_LINK ...) at configure time — always
# absolute-path, tied to whatever machine configured the build, so must never be committed.
/public/audio
/public/Eden.eden

# OS
.DS_Store
